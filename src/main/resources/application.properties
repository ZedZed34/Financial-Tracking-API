spring.application.name=Financial-API
server.port=3000

# Run with in-memory DB by default so "mvn spring-boot:run" works out of the box.
# Override with: -Dspring-boot.run.profiles=default (Postgres) or set SPRING_PROFILES_ACTIVE
spring.profiles.active=dev
